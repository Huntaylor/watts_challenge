import 'package:flutter/widgets.dart';

class TimerHUD extends StatelessWidget {
  const TimerHUD({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text(''),
    );
  }
}
